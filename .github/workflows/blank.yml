name: Build & deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: sainiji
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
     
    - name: Build project
      run: ls -l
    
   
